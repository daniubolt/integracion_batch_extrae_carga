#!/bin/bash

#PS_RESULT=`ps -ea | grep -c run.procedures`
PS_RESULT=`ps -ea | grep run.procedures | awk 'END{print NR - 2}'`

echo "$PS_RESULT"

if [ "$PS_RESULT" -eq 0 ]
then
	java -jar executeProcedure.jar
else
	echo "El proceso sigue ejecutando"
fi